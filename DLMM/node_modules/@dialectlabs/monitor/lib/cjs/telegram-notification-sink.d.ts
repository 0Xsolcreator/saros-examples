import { Notification, ResourceId } from './data-model';
import { NotificationSink, NotificationSinkMetadata, SubscriberRepository } from './ports';
import { NotificationTypeEligibilityPredicate } from './internal/notification-type-eligibility-predicate';
/**
 * Telegram notification
 */
export interface TelegramNotification extends Notification {
    body: string;
}
export declare class TelegramNotificationSink implements NotificationSink<TelegramNotification> {
    private readonly telegramBotToken;
    private readonly subscriberRepository;
    private readonly notificationTypeEligibilityPredicate;
    private bot;
    constructor(telegramBotToken: string, subscriberRepository: SubscriberRepository, notificationTypeEligibilityPredicate: NotificationTypeEligibilityPredicate);
    push(notification: TelegramNotification, recipients: ResourceId[], { notificationMetadata }: NotificationSinkMetadata): Promise<void>;
}
