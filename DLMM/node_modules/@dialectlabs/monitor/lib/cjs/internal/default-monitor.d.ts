import { DataSourceTransformationPipeline, PushyDataSource, SubscriberRepository } from '../ports';
import { Monitor } from '../monitor-api';
export declare class DefaultMonitor<T extends Object> implements Monitor<T> {
    private readonly dataSource;
    private readonly dataSourceTransformationPipelines;
    private readonly subscriberRepository;
    private started;
    private subscriptions;
    constructor(dataSource: PushyDataSource<T>, dataSourceTransformationPipelines: DataSourceTransformationPipeline<T, any>[], subscriberRepository: SubscriberRepository);
    start(): Promise<void>;
    stop(): Promise<void>;
    private startMonitorPipeline;
    private enrichWithContext;
}
