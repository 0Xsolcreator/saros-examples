import { Subscriber } from '../ports';
import { NotificationMetadata } from '../monitor-builder';
export declare abstract class NotificationTypeEligibilityPredicate {
    abstract isEligible(subscriber: Subscriber, metadata?: NotificationMetadata): Boolean;
    static create(): DefaultNotificationTypeEligibilityPredicate;
}
export declare class DefaultNotificationTypeEligibilityPredicate extends NotificationTypeEligibilityPredicate {
    isEligible({ notificationSubscriptions }: Subscriber, metadata?: NotificationMetadata): Boolean;
}
