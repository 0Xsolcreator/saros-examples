import { Notification, ResourceId } from './data-model';
import { NotificationSink, NotificationSinkMetadata, SubscriberRepository } from './ports';
import { NotificationTypeEligibilityPredicate } from './internal/notification-type-eligibility-predicate';
/**
 * Email notification
 */
export interface EmailNotification extends Notification {
    subject: string;
    text: string;
}
export declare class SengridEmailNotificationSink implements NotificationSink<EmailNotification> {
    private readonly sengridApiKey;
    private readonly senderEmail;
    private readonly subscriberRepository;
    private readonly notificationTypeEligibilityPredicate;
    constructor(sengridApiKey: string, senderEmail: string, subscriberRepository: SubscriberRepository, notificationTypeEligibilityPredicate: NotificationTypeEligibilityPredicate);
    push(notification: EmailNotification, recipients: ResourceId[], { notificationMetadata }: NotificationSinkMetadata): Promise<void>;
}
