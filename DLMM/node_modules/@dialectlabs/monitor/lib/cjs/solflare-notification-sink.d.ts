import { Notification, ResourceId } from './data-model';
import { NotificationSink } from './ports';
/**
 * Solflare notification
 */
export interface SolflareNotification extends Notification {
    title: string;
    body: string;
    actionUrl: string | null;
}
export declare class SolflareNotificationSink implements NotificationSink<SolflareNotification> {
    private readonly solcastApiKey;
    private readonly solcastEndpoint;
    constructor(solcastApiKey: string, solcastEndpoint?: string);
    push({ title, body, actionUrl }: SolflareNotification, publicKeys: ResourceId[]): Promise<void>;
}
