import { Subscriber, SubscriberEventHandler, SubscriberRepository } from './ports';
import { BlockchainSdk, DialectSdk } from '@dialectlabs/sdk';
export declare class DialectSdkSubscriberRepository implements SubscriberRepository {
    private sdk;
    private dapp;
    constructor(sdk: DialectSdk<BlockchainSdk>);
    subscribe(onSubscriberAdded?: SubscriberEventHandler, onSubscriberRemoved?: SubscriberEventHandler): void;
    findAll(): Promise<Subscriber[]>;
    private findAddressSubscribers;
    private findNotificationTypeSubscribers;
    private lookupDapp;
}
