import { Notification, ResourceId } from './data-model';
import { NotificationSink, NotificationSinkMetadata, SubscriberRepository } from './ports';
import { NotificationTypeEligibilityPredicate } from './internal/notification-type-eligibility-predicate';
/**
 * Sms notification
 */
export interface SmsNotification extends Notification {
    body: string;
}
export declare class TwilioSmsNotificationSink implements NotificationSink<SmsNotification> {
    private readonly twilioAccount;
    private readonly senderSmsNumber;
    private readonly subscriberRepository;
    private readonly notificationTypeEligibilityPredicate;
    private twilio;
    constructor(twilioAccount: {
        username: string;
        password: string;
    }, senderSmsNumber: string, subscriberRepository: SubscriberRepository, notificationTypeEligibilityPredicate: NotificationTypeEligibilityPredicate);
    push(notification: SmsNotification, recipients: ResourceId[], { notificationMetadata }: NotificationSinkMetadata): Promise<void>;
}
